<%= form_for(@smartphone) do |f| %>
  <% if @smartphone.errors.any? %>
    <div id="error_explanation" class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@smartphone.errors.count, "error") %> prohibited this listing from being saved:</h4>

      <ul>
      <% @smartphone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :marque %><br>
    <%= f.text_field :marque, :class => "form-control", :placeholder => "Marque (ex: Apple, Samsung...)" %>
  </div>
  <div class="form-group">
    <%= f.label :modele %><br>
    <%= f.text_field :modele, :class => "form-control", :placeholder => "Modèle (ex: iPhone 5, Galaxy S4...)" %>
  </div>
  <div class="form-group">
	<%= f.label "Choisissez une image" %><br>
    <%= f.file_field :image, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
